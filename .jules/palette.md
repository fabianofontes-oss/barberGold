## 2025-02-18 - Missing Empty States Pattern
**Learning:** List views (Search, Clients) frequently lack explicit "empty states", leaving users confused when data is missing or search yields no results.
**Action:** When implementing or refactoring lists, always define and implement two empty states: "No data yet" (onboarding) and "No results found" (filtering).
